---
phase: 10-constructor-spec-declaration-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [SFMK-01, SFMK-02, SFMK-03]
must_haves:
  truths:
    - "ComposedSpecClassDeclaration.Build() with non-null containingTypeName produces constructor spec classes using only SyntaxFactory (no ParseExpression, ParseCompilationUnit, or StringBuilder)"
    - "BuildParameterList() constructs the primary constructor parameter via SyntaxFactory.ParameterList with ParseTypeName for the type"
    - "ExpressionTransformer.PrefixInstanceMethods operates on ExpressionSyntax nodes structurally (no string manipulation for instance. prefix)"
    - "All 94 CodeFix tests pass unchanged including the 4+ constructor-specific tests"
  artifacts:
    - path: "src/Motiv.CodeFix/Syntax/ComposedSpecClassDeclaration.cs"
      provides: "Constructor spec class generation via SyntaxFactory"
      contains: "BuildParameterList"
    - path: "src/Motiv.CodeFix/LogicalExpressionToSpecConverter.cs"
      provides: "Constructor spec orchestration with containingTypeName"
      contains: "containingTypeName"
  key_links:
    - from: "LogicalExpressionToSpecConverter"
      to: "ComposedSpecClassDeclaration"
      via: "containingTypeName parameter passed to constructor"
      pattern: "new ComposedSpecClassDeclaration.*containingTypeName"
    - from: "ComposedSpecClassDeclaration.BuildParameterList"
      to: "Generated spec class"
      via: "ParameterList with instance parameter"
      pattern: "Parameter.*Identifier.*instance.*WithType.*ParseTypeName"
---

<objective>
Verify that constructor spec declaration migration (SFMK-01, SFMK-02, SFMK-03) is already complete from Phases 8/9.

Purpose: The research phase confirmed that the constructor spec path flows through ComposedSpecClassDeclaration.Build() with a non-null containingTypeName, which was fully migrated to SyntaxFactory in Phases 8 and 9. This plan performs code inspection and test execution to formally close all three requirements without new implementation.

Output: Verified requirements, no code changes expected.
</objective>

<execution_context>
@C:/Users/karls/.claude-home/get-shit-done/workflows/execute-plan.md
@C:/Users/karls/.claude-home/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-constructor-spec-declaration-migration/10-RESEARCH.md
@.planning/phases/09-composed-spec-declaration-migration/09-01-SUMMARY.md
@.planning/phases/09-composed-spec-declaration-migration/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Code inspection — confirm no string-based code generation in constructor spec path</name>
  <files>src/Motiv.CodeFix/Syntax/ComposedSpecClassDeclaration.cs, src/Motiv.CodeFix/LogicalExpressionToSpecConverter.cs, src/Motiv.CodeFix/Syntax/SpecClassDeclaration.cs</files>
  <action>
Inspect the constructor spec code path to confirm SFMK-01 and SFMK-02 are satisfied:

1. Read `src/Motiv.CodeFix/Syntax/ComposedSpecClassDeclaration.cs` and verify:
   - `BuildParameterList()` uses `ParameterList(SingletonSeparatedList(Parameter(Identifier("instance")).WithType(ParseTypeName(containingTypeName))))` when `containingTypeName` is non-null
   - No calls to `ParseExpression`, `ParseCompilationUnit`, `ParseStatement`, or `StringBuilder` exist anywhere in the file
   - `AttachLambdaBody`, `AddClassBody`, and `GenerateClauseStatementSyntaxes` use only SyntaxFactory

2. Read `src/Motiv.CodeFix/LogicalExpressionToSpecConverter.cs` and verify:
   - `BuildSingleVarComposedSpec` passes `containingTypeName` to `ComposedSpecClassDeclaration` constructor
   - `BuildMultiVarComposedSpec` passes `containingTypeName` to `ComposedSpecClassDeclaration` constructor
   - `ExpressionTransformer.PrefixInstanceMethods` is used for instance method injection (structural, not string-based)

3. Read `src/Motiv.CodeFix/Syntax/SpecClassDeclaration.cs` and verify the abstract base has no string-based code generation

4. Grep across all files in `src/Motiv.CodeFix/Syntax/` for any remaining `ParseExpression`, `ParseCompilationUnit`, `ParseStatement`, or `StringBuilder` calls. Report findings.

If any string-based generation is found in the constructor spec path, flag it as a gap. If none found, confirm SFMK-01 and SFMK-02 are satisfied.
  </action>
  <verify>
    <automated>cd /c/Dev/Motiv && grep -rn "ParseExpression\|ParseCompilationUnit\|ParseStatement\|StringBuilder" src/Motiv.CodeFix/Syntax/ComposedSpecClassDeclaration.cs src/Motiv.CodeFix/Syntax/SpecClassDeclaration.cs || echo "PASS: No string-based code generation found"</automated>
  </verify>
  <done>Code inspection confirms no ParseExpression, ParseCompilationUnit, ParseStatement, or StringBuilder calls exist in ComposedSpecClassDeclaration or SpecClassDeclaration. BuildParameterList uses SyntaxFactory for constructor parameter construction. SFMK-01 and SFMK-02 verified.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite — confirm SFMK-03 (all constructor spec tests pass unchanged)</name>
  <files></files>
  <action>
Run the CodeFix test suite to confirm SFMK-03:

1. Run `dotnet test src/Motiv.CodeFix.Tests/Motiv.CodeFix.Tests.csproj -q` and confirm all 94 tests pass.

2. Specifically verify the constructor-specific tests pass:
   - `Should_inject_instance_via_constructor_when_expression_calls_instance_methods`
   - `Should_convert_multiple_variables_with_instance_methods_and_generate_model`
   - `Should_convert_complex_inside_a_namespace_block_and_handle_multiple_clauses_with_instance_methods`
   - `Should_convert_instance_methods_with_debug_tap` (in debug output tests)

3. Run the full solution test suite on net10.0: `dotnet test /c/Dev/Motiv/Motiv.sln -q` to confirm no regressions.

If all tests pass, SFMK-03 is confirmed satisfied.
  </action>
  <verify>
    <automated>cd /c/Dev/Motiv && dotnet test src/Motiv.CodeFix.Tests/Motiv.CodeFix.Tests.csproj -q</automated>
  </verify>
  <done>All 94 CodeFix tests pass including the 4 constructor-specific tests. SFMK-03 verified — all existing constructor spec tests pass unchanged.</done>
</task>

</tasks>

<verification>
All three SFMK requirements verified:
- SFMK-01: Code inspection confirms ComposedSpecClassDeclaration.Build() with containingTypeName uses only SyntaxFactory
- SFMK-02: BuildParameterList() and ExpressionTransformer.PrefixInstanceMethods use SyntaxFactory node construction
- SFMK-03: All 94 CodeFix tests pass including constructor-specific tests
</verification>

<success_criteria>
1. No ParseExpression, ParseCompilationUnit, ParseStatement, or StringBuilder calls found in ComposedSpecClassDeclaration.cs or SpecClassDeclaration.cs
2. BuildParameterList() confirmed to use SyntaxFactory.ParameterList for constructor parameter
3. All 94 CodeFix tests pass
4. Constructor-specific tests explicitly confirmed passing
</success_criteria>

<output>
After completion, create `.planning/phases/10-constructor-spec-declaration-migration/10-01-SUMMARY.md`
</output>
