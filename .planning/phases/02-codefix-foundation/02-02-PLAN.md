---
phase: 02-codefix-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/Motiv.CodeFix/MotivCodeFixProvider.cs
  - src/Motiv.CodeFix/ConvertToSpecCodeFix.cs
  - src/Motiv.CodeFix.Tests/MotivConvertToSpecTests.cs
autonomous: true

must_haves:
  truths:
    - "CodeFix generates AgeProposition/AgeModel for expression age > 18 (not hard-coded Proposition/Model)"
    - "CodeFix generates OrderProposition/OrderModel for expression order.Total > 100 && order.IsActive"
    - "CodeFix generates Proposition/Model fallback for unrelated variables"
    - "Generated code contains no Debug.WriteLine calls in any path"
    - "Generated code does not add using System.Diagnostics"
    - "Original expression is preserved as a comment in generated code"
  artifacts:
    - path: "src/Motiv.CodeFix/MotivCodeFixProvider.cs"
      provides: "CodeFix entry point that derives names from expression content"
      contains: "ExpressionNameDeriver"
    - path: "src/Motiv.CodeFix/ConvertToSpecCodeFix.cs"
      provides: "Conversion logic without Debug.WriteLine generation"
    - path: "src/Motiv.CodeFix.Tests/MotivConvertToSpecTests.cs"
      provides: "End-to-end CodeFix tests verifying derived names and no Debug output"
      contains: "Should_derive"
  key_links:
    - from: "src/Motiv.CodeFix/MotivCodeFixProvider.cs"
      to: "src/Motiv.CodeFix/ExpressionNameDeriver.cs"
      via: "calls DeriveClassNames with expression and semantic model"
      pattern: "ExpressionNameDeriver\\.DeriveClassNames"
    - from: "src/Motiv.CodeFix/MotivCodeFixProvider.cs"
      to: "src/Motiv.CodeFix/ConvertToSpecCodeFix.cs"
      via: "passes derived names to LogicalExpressionToSpecConverter constructor"
      pattern: "new LogicalExpressionToSpecConverter\\("
    - from: "src/Motiv.CodeFix/ConvertToSpecCodeFix.cs"
      to: "generated code template"
      via: "multi-variable method body template string"
      pattern: "Debug\\.WriteLine"
---

<objective>
Wire ExpressionNameDeriver into the CodeFix pipeline, remove Debug.WriteLine from generated output, and update all tests to verify the new behavior.

Purpose: This is the integration plan that makes the CodeFix actually produce clean, context-aware output. Plan 01 created the algorithm; this plan connects it to the CodeFix and ensures the full pipeline works end-to-end.

Output: Updated CodeFix that generates context-derived names and clean code, with comprehensive end-to-end tests.
</objective>

<execution_context>
@C:\Users\karls\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\karls\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Dev\Motiv\.planning\PROJECT.md
@C:\Dev\Motiv\.planning\ROADMAP.md
@C:\Dev\Motiv\.planning\STATE.md
@C:\Dev\Motiv\.planning\phases\02-codefix-foundation\02-CONTEXT.md
@C:\Dev\Motiv\.planning\phases\02-codefix-foundation\02-RESEARCH.md
@C:\Dev\Motiv\.planning\phases\02-codefix-foundation\02-01-SUMMARY.md
@C:\Dev\Motiv\src\Motiv.CodeFix\MotivCodeFixProvider.cs
@C:\Dev\Motiv\src\Motiv.CodeFix\ConvertToSpecCodeFix.cs
@C:\Dev\Motiv\src\Motiv.CodeFix\ExpressionNameDeriver.cs
@C:\Dev\Motiv\src\Motiv.CodeFix.Tests\MotivConvertToSpecTests.cs
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED -- Write failing end-to-end CodeFix tests with derived names and no Debug</name>
  <files>src/Motiv.CodeFix.Tests/MotivConvertToSpecTests.cs</files>
  <action>
Update the existing test file to reflect the new expected behavior. The existing 4 tests currently expect hard-coded `Proposition`/`Model` names and include `Debug.WriteLine` in expected output. Update them AND add new tests.

**Update existing tests:**

1. `Should_convert_single_variable_boolean_expressions_that_can_be_converted_to_spec`:
   - Expression is `value > 0` with parameter `int value`
   - Expected proposition name: `ValueProposition` (derived from `value`, PascalCased + suffix)
   - Expected invocation: `new ValueProposition().IsSatisfiedBy(value).Satisfied`
   - Expected class: `public class ValueProposition() : Spec<int>(...)`
   - NO Debug.WriteLine anywhere, NO `using System.Diagnostics`

2. `Should_convert_double_variable_boolean_return_expressions_that_can_be_converted_to_spec`:
   - Expression is `valueA > valueB && valueC`
   - These are 3 unrelated variables (valueA, valueB, valueC all appear once). Per user decision, fallback to `Proposition`/`Model`.
   - Remove `Debug.WriteLine(result.Reason);` from expected output
   - Remove `using System.Diagnostics;` from expected output
   - Keep the `// {expression}` comment (original expression preserved)

3. `Should_convert_double_variable_boolean_expression_statement_that_can_be_converted_to_spec`:
   - Same expression pattern, same fallback to `Proposition`/`Model`
   - Remove `Debug.WriteLine(result.Reason);` from expected output
   - Remove `using System.Diagnostics;` from expected output

4. `Should_convert_many_nested_clauses_into_a_spec`:
   - Expression: `valueA >= 0 && (valueB >= 0 || !(valueC >= 1^valueC <= 10))`
   - Variables: valueA (1x), valueB (1x), valueC (2x) -- valueC is the common root (most frequent)
   - Expected names: `ValueCProposition`, `ValueCModel`
   - Remove `Debug.WriteLine(result.Reason);` from expected output
   - Remove `using System.Diagnostics;` from expected output

**Add new tests for name derivation scenarios:**

5. `Should_derive_name_from_member_access_root`:
   - Source: method with `Order order` parameter, expression `order.Total > 100`
   - Expected: `OrderProposition` class, single-variable path
   - Need a class `Order` with `public int Total { get; set; }` in the test source

6. `Should_derive_common_root_from_multiple_member_accesses`:
   - Source: method with `Order order` parameter, expression `order.Total > 100 && order.IsActive`
   - Expected: `OrderProposition`, `OrderModel` (composed, multi-variable -- wait, this is actually single-variable since `order` is one parameter)
   - Actually `order.Total > 100 && order.IsActive` uses one variable `order`, so it hits the single-variable path. Expected: `OrderProposition` class with `Spec<Order>`.

7. `Should_derive_name_from_is_expression_variable`:
   - Source: method with `object obj` parameter, expression `obj is string`
   - Expected: `ObjProposition` (derived from `obj`, not `string`) -- LOCKED decision
   - This is a single-variable expression, so single-variable path

8. `Should_use_fallback_name_for_unrelated_variables`:
   - Source: method with `int x, int y` parameters, expression `x > 5 && y < 10`
   - Expected: `Proposition`, `Model` (fallback -- LOCKED decision)
   - Multi-variable path, no Debug.WriteLine

**Important test pattern notes:**
- All expected outputs must NOT contain `Debug.WriteLine` or `using System.Diagnostics` -- LOCKED decision
- All expected outputs SHOULD preserve the `// {expression}` comment for multi-variable path
- Follow the existing VerifyCS.Test pattern exactly
- For tests needing custom types (Order), add the type definition in the test source string

Run all tests -- new/updated tests MUST fail (CodeFix still generates old names and Debug.WriteLine).
Commit: `test(02-02): update CodeFix tests for derived names and clean output`
  </action>
  <verify>
Run `dotnet test src/Motiv.CodeFix.Tests` -- tests should fail because CodeFix still produces hard-coded "Proposition"/"Model" and includes Debug.WriteLine.
  </verify>
  <done>Test file contains 8+ tests. All new/updated tests fail with mismatched expected output (wrong names, Debug.WriteLine present).</done>
</task>

<task type="auto">
  <name>Task 2: GREEN -- Integrate ExpressionNameDeriver, remove Debug.WriteLine, make all tests pass</name>
  <files>src/Motiv.CodeFix/MotivCodeFixProvider.cs, src/Motiv.CodeFix/ConvertToSpecCodeFix.cs</files>
  <action>
Make all tests pass by wiring ExpressionNameDeriver into the CodeFix and removing Debug.WriteLine generation.

**Changes to MotivCodeFixProvider.cs:**

1. In `RegisterCodeFixesAsync`, instead of hard-coding `new LogicalExpressionToSpecConverter("Proposition", "Model", context.Document)`:
   - Get the semantic model: `var semanticModel = await context.Document.GetSemanticModelAsync(context.CancellationToken);`
   - For each diagnostic, after finding the `expressionSyntax`:
     - Call `ExpressionNameDeriver.DeriveClassNames(expressionSyntax, semanticModel, expressionSyntax.SpanStart)` to get derived names
     - Pass the derived `propositionName` and `modelName` to `new LogicalExpressionToSpecConverter(propositionName, modelName, context.Document)`
   - The converter instantiation moves INSIDE the foreach loop (each expression may derive different names)

**Changes to ConvertToSpecCodeFix.cs (LogicalExpressionToSpecConverter):**

1. **Remove Debug.WriteLine from multi-variable template** (line ~258):
   - In `ReplaceMultiVariableExpression`, the `newMethodSource` string template currently contains `Debug.WriteLine(result.Reason);`
   - Remove this line entirely from the template
   - The method body should go directly from `var result = ...;` to the assignment line

2. **Remove Debug.WriteLine from `CreateMethodWithPropositionLogic`** (line ~302):
   - Same pattern -- remove `Debug.WriteLine(result.Reason);` from the `newCode` string template

3. **Update `AddUsingStatementsIfNeeded`**:
   - Remove the `includeSystemDiagnostics` parameter entirely
   - Remove the `System.Diagnostics` using directive addition logic (lines 334-336)
   - The method should ONLY add `using Motiv;` when needed
   - Update the method signature: `private static SyntaxNode AddUsingStatementsIfNeeded(SyntaxNode newRoot)`
   - Update the call site (line 52) to remove the boolean argument

4. **Update the call to `AddUsingStatementsIfNeeded`** (line ~52):
   - Change from `AddUsingStatementsIfNeeded(newRoot, variableSymbols.Length > 1)` to `AddUsingStatementsIfNeeded(newRoot)`

**Verify comment preservation:**
- The `// {originalExprText}` comment line in the multi-variable template should remain -- this is a Claude discretion item and the existing pattern is good

Run ALL tests (not just new ones).
Commit: `feat(02-02): integrate name derivation and remove Debug.WriteLine from CodeFix`
  </action>
  <verify>
Run `dotnet test src/Motiv.CodeFix.Tests` -- ALL tests pass (both ExpressionNameDeriver unit tests from Plan 01 and end-to-end CodeFix tests).
Run `dotnet build src/Motiv.CodeFix` -- no compilation errors.
Grep for `Debug.WriteLine` in ConvertToSpecCodeFix.cs -- should return zero matches in string templates.
Grep for `System.Diagnostics` in AddUsingStatementsIfNeeded -- should not be adding it.
  </verify>
  <done>
All CodeFix tests pass with:
- Context-derived proposition/model names (ValueProposition, OrderProposition, etc.)
- Fallback to Proposition/Model for unrelated variables
- No Debug.WriteLine in any generated code
- No using System.Diagnostics added
- Original expression comment preserved in multi-variable path
  </done>
</task>

</tasks>

<verification>
- `dotnet test src/Motiv.CodeFix.Tests` -- ALL tests pass (unit + end-to-end)
- `dotnet build` from solution root -- full solution compiles
- No `Debug.WriteLine` in CodeFix-generated code (verify by reading ConvertToSpecCodeFix.cs string templates)
- No `System.Diagnostics` import logic in AddUsingStatementsIfNeeded
- Test coverage includes: single var naming, member access roots, common root frequency, is-expression variable derivation, fallback for unrelated vars, no Debug.WriteLine in output
</verification>

<success_criteria>
- MotivCodeFixProvider calls ExpressionNameDeriver.DeriveClassNames to derive names from expression content
- Generated proposition/model names are context-aware (not hard-coded)
- Generated code contains zero Debug.WriteLine calls
- Generated code does not add using System.Diagnostics
- Original expression comment is preserved
- 8+ end-to-end CodeFix tests pass covering all naming scenarios
- Full solution builds and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-codefix-foundation/02-02-SUMMARY.md`
</output>
